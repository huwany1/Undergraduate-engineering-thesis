# 项目开发规则与代理协作规范 (Project Development Rules & Agent Guidelines)

本文档定义了本项目在功能开发、缺陷修复、架构设计、文档编制及 Agent 协作中的核心规范与强制性执行标准。所有自主智能体（Master Agent 及 Sub-Agents）与开发者均须严格遵守。

---

## 一、测试驱动与防回归规则 (Test-Driven & Regression Prevention)

### 1.1 核心原则
- **有变更必有测试**：在进行任何新功能开发（Feature Development）或缺陷修复（Bug Fix）时，**必须同步编写或更新自动化测试**（存放于 `tests/` 目录）。
- **禁止无测试交付**：未配备对应测试代码的功能或修复不得视为完成。

### 1.2 Bug 场景固化 (Scenario Pinning)
- **精准复现**：针对所有修复的 Bug，必须首先编写能够精准复现该故障场景的回归测试用例（Regression Test Case）。
- **边界与断言固化**：在测试中固定异常输入、边界条件、并发或时序状态，并显式断言修复后的正确预期。
- **固化为资产**：该测试用例将作为项目永久的测试资产被纳入代码库，不可随意删除或跳过。

### 1.3 CI 自动化兜底与防回归 (CI Gatekeeper)
- **CI 强制阻断**：所有提交与 Pull Request 均会自动触发 GitHub Actions 持续集成流水线（`.github/workflows/ci.yml`），自动执行：
  ```bash
  uv run pytest -v tests/
  ```
- **零容忍回归**：若未来的任何代码重构或新改动将已修复的 Bug 重新引入，CI 流水线必须直接报错挂掉（Fail Fast），坚决阻断合并，彻底杜绝逻辑回归与隐性破坏。

---

## 二、信息审查、架构指标与多 Agent 对抗性审评流程

在启动任何重大修改、代码重构或新模块实现前，必须严格遵循以下四阶段闭环工作流：

### 2.1 阶段一：MCP 与 SKILL 自动化审查 (Information Discovery & Verification)
- **主动调用**：必须主动并自动调用相关 MCP 工具与对应领域 SKILL（如现代 Web、Python 依赖管理、机器学习最佳实践等），检索官方文档、代码基线与真实 API 规范。
- **审查可靠性**：严禁凭空揣测或使用未经核实的代码模式，必须对信息来源与技术可行性进行严格审查，确保证据链确凿。

### 2.2 阶段二：深度思考与五大架构指标对齐 (Deep Thinking & Architecture Metrics)
经过严谨分析后进行深度思考，制定的技术方案必须显式评估并**至少严格满足以下五大软件工程指标中的一个或多个**（可选、可多选，最少满足一项）：
1. **低耦合 (Low Coupling)**：模块间依赖极小化、接口清晰抽象，隔离变化与副作用。
2. **高内聚 (High Cohesion)**：模块职责聚焦明确，相关数据与操作紧密围绕单一职责。
3. **高可用 (High Availability)**：具备容错性、优雅降级、异常隔离与故障自愈能力。
4. **高性能 (High Performance)**：计算复杂度优化、低延迟、低内存占用及资源高效利用。
5. **可维护 (Maintainability)**：代码结构清晰、可读性强、规范统一、极易扩展与排错。

方案设计中必须明确标注本方案重点保障的指标及其具体实现技术策略。

### 2.3 阶段三：多子 Agent 多轮对抗性审评 (Multi-Agent Adversarial Reviews)
在方案进入执行前，必须开启**多个子 Agents** 展开多轮对抗性 Review：
- **角色分离**：
  - **提案者 Agent (Proposer)**：阐述架构设计、技术选型与五大指标达成路径。
  - **批判/红队 Agent (Challenger/Red Team)**：扮演挑刺者与破坏者，专职挖掘极端边界场景、隐式死锁、并发竞态、内存膨胀、异常逃逸及违背低耦合高内聚的设计缺陷。
- **多轮对抗辩论**：围绕系统瓶颈、潜在风险点进行多轮质询与防御论证。
- **共识与方案综合**：综合双方审评结果，吸纳批判性反馈进行方案迭代，最终形成收敛且经受住推敲的成熟方案。

### 2.4 阶段四：落地实现与双向验证
- 只有在多 Agent 对抗性审评达成共识后，方可着手进行具体文件的修改与代码实现。
- 实现完成后，立即运行本地测试，并确保通过全部自动化测试用例，方可输出交付结果。

---

## 三、设计与方案文档编制规范 (Documentation & Aesthetics Standards)

### 3.1 代码解耦与伪代码备忘 (Code Decoupling & Pseudocode Memo)
- **实现代码解耦**：方案与架构设计类的 Markdown 文档原则上**无需输出完整实现代码**。具体代码应由后续开发阶段直接在源码文件中编写与落地，避免方案文档冗长且与真实代码演进脱节。
- **伪代码与备忘录机制**：若方案需要明确算法时序、状态流转或数据契约结构，应采用**轻量抽象伪代码 (Pseudocode)**、类型签名或**设计备忘录 (Design Memo)** 的形式呈现核心逻辑骨架，重点聚焦于逻辑正确性与边界控制。

### 3.2 文档美化与排版规范 (Aesthetics & Polished Typography)
- **先美化再输出**：所有对外交付或沉淀归档的 Markdown 文档，**必须先完成深度排版与视觉美化后再进行输出**，杜绝粗制滥造的纯文字堆砌。
- **美化要素要求**：
  - **结构清晰**：合理的章节编号、多级标题导读及清晰的段落视觉节奏。
  - **视觉强调**：合理运用 GitHub 风格提示块（`> [!NOTE]`, `> [!TIP]`, `> [!IMPORTANT]`, `> [!WARNING]`, `> [!CAUTION]`）对关键假设、风险点及决策依据进行醒目标注。
  - **图表增强**：系统拓扑、状态转换、时序交互优先使用标准 Mermaid 图（Flowchart, SequenceDiagram, StateDiagram）进行可视化展示。
  - **规整对比**：架构选型、方案对比、门禁矩阵等必须组织为标准 Markdown 表格，确保可读性与专业质感。

---

## 四、本地分支与 Git 提交全生命周期规范 (Git Lifecycle & Commit Standards)

### 4.1 启动前：排查未提交重大修改 (Pre-flight Status Check)
- **前置工作区核查**：在启动任何新任务、方案设计、模块编写或缺陷修复前，**必须首先执行 `git status` 检查当前本地分支状态**。
- **未提交成果排查**：严格确认是否存在前序任务未提交的代码、测试或文档改动。若发现存在未提交的重大工作成果，必须优先与用户确认并妥善处理（归档、提交或清理隔离），坚决禁止在未知/脏版本基准上叠加修改，防止上下文混乱与劳动成果丢失。

### 4.2 交付后：修改强制提交到本地分支 (Post-completion Branch Commit)
- **任务闭环提交**：任何任务、功能开发、Bug Fix 或文档规则的修改一旦验证完成（本地测试、门禁核验全部 PASS），**必须主动将本次修改作为一次提交存入本地分支**。
- **杜绝工作区悬空**：时刻保持工作区整洁清晰，实现“修改有验证，交付必入库”，确保开发历程在 Git 历史中清晰可追溯。

### 4.3 提交信息详尽描述规范 (Detailed Commit Description)
- **严禁简陋提交**：禁止使用诸如 `"update"`, `"fix"`, `"commit"` 等单一无信息的模糊 Commit Message。
- **结构化提交信息要求**：每一个提交必须包含结构完整、表达自洽、详尽具体的说明：
  1. **标题行 (Header)**：采用语义化约定（如 `feat(...)`, `fix(...)`, `docs(...)`, `refactor(...)`, `test(...)`, `chore(...)`），简明阐述变更核心（50~72 字符）。
  2. **变更背景与动因 (Why)**：详述为何做此修改、遵循了什么规范、解决了什么场景或阻断了什么潜在缺陷。
  3. **核心改动清单 (What)**：逐项列出本次提交涉及的关键文件、模块及主要修改内容。
  4. **验证与测试结果 (Verification)**：记录本地自动化测试通过情况（如 `pytest` 执行用例数、门禁脚本验证结果等）。
